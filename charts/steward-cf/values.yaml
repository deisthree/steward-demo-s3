# Default values for s3-service-provider.
# This is a YAML-formatted file.
# Declare name/value pairs to be passed into your templates.
# name: value

# Image: "quay.io/arschles/steward-cf"
# ImageTag: "devel"
Image: "192.168.99.100:5000/krancour/steward-cf"
ImageTag: "git-658e6bd"
ImagePullPolicy: "Always"

Replicas: 1

LogLevel: "trace"
Port: 8080
